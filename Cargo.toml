[package]
name = "macwin-debug-mvp"
version = "0.1.0"
edition = "2021"
rust-version = "1.94"
license = "MIT"
description = "macwin-debug-mvp — cross-platform IPC daemon scaffolded by macwin"

[[bin]]
name = "macwin-debug-mvp"
path = "src/main.rs"

[lib]
name = "macwin_debug_mvp"
path = "src/lib.rs"

[dependencies]
# Cross-platform IPC: Windows named pipes + Unix domain sockets behind one
# unified API. Same code, every macwin target.
interprocess = "2"

[profile.release]
lto = "thin"
strip = true
